---
const { user } = Astro.locals;
---

<div
  class="border-border bg-card/80 text-foreground mb-4 flex items-center justify-between rounded-xl border px-4 py-2 text-sm shadow-sm backdrop-blur-sm"
>
  {
    user ? (
      <>
        <span class="text-muted-foreground">{user.email}</span>
        <div class="flex items-center gap-3">
          <a href="/dashboard" class="text-accent-strong hover:text-primary transition-colors hover:underline">
            Panel
          </a>
          <form method="POST" action="/api/auth/signout">
            <button type="submit" class="text-accent-strong hover:text-primary transition-colors hover:underline">
              Wyloguj
            </button>
          </form>
        </div>
      </>
    ) : (
      <>
        <span class="text-muted-foreground">Nie zalogowano</span>
        <div class="flex gap-3">
          <a href="/auth/signin" class="text-accent-strong hover:text-primary transition-colors hover:underline">
            Zaloguj się
          </a>
          <a href="/auth/signup" class="text-accent-strong hover:text-primary transition-colors hover:underline">
            Załóż konto
          </a>
        </div>
      </>
    )
  }
</div>
